{
  "extends": "./tsconfig.json",
  // "include": ["src/**/*", "../../packages/shared/**/*"], // ✅ shared 추가
  // "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
  "exclude": [
    "node_modules",
    "../../node_modules",
    "test",
    "dist",
    "**/*spec.ts"
  ]
}
